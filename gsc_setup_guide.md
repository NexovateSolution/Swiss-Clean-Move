# Google Search Console (GSC) Setup & Indexing Guide
## SwissCleanMove Seeland SEO Expansion

This guide outlines the steps to track and force the indexing of the 21 new Seeland regional SEO pages to quickly gain visibility on Google.

### Phase 1: Property Verification
If not already done, verify your domain (`swisscleanmove.ch`) in Google Search Console using the **Domain property** method (via DNS TXT record). This is the most reliable method and covers all subdomains and protocols.

### Phase 2: XML Sitemap Submission
1. Ensure your Next.js app generates a dynamic `sitemap.xml` that includes all localized versions of the 21 regional pages (de, en, fr).
2. Go to **Sitemaps** in the left menu of GSC.
3. Enter the URL of your sitemap (e.g., `https://swisscleanmove.ch/sitemap.xml`) and click **Submit**.

### Phase 3: Priority Indexing (Manual Submission)
To accelerate the indexing of your new high-value regional pages, manually request indexing for the primary language (German) URLs:

1. Go to the top search bar in GSC ("Inspect any URL in...").
2. Paste the URL of the most important new pages, e.g., `https://swisscleanmove.ch/de/umzug-biel` and hit Enter.
3. Click **Request Indexing**.
4. Repeat this for the top 5-10 pillar pages.

**Top Priority Pages to Submit:**
* `/de/umzug-biel`
* `/de/umzug-lyss`
* `/de/endreinigung-biel`
* `/de/endreinigung-aarberg`
* `/de/reinigung-nidau`

### Phase 4: Monitoring Setup
Set up custom tracking to monitor the performance of your new keyword strategy:

1. **Performance Report Filtering:** In GSC -> Performance, create a "Page" filter containing `/de/umzug-` or `/de/endreinigung-` to isolate traffic going to your new landing pages.
2. **Keyword Tracking:** In the same report, look at the "Queries" tab. You should start seeing impressions for:
   * "Umzugsfirma Biel"
   * "Endreinigung Aarberg"
   * "Umzug Seeland"
3. **Core Web Vitals:** Monitor the "Experience" -> "Core Web Vitals" tab to ensure the new Image SEO and lazy loading updates are registering as "Good" URLs (LCP < 2.5s).

### Phase 5: Google Analytics (GA4) Integration
We have integrated conversion tracking on the CTA buttons and the final form submission.
Ensure your GA4 property is linked to your GSC property to see search queries and landing page behavior in a single dashboard.

**Events currently tracked:**
* `click_cta` (Label: `hero_form_btn`, `hero_phone_btn`, etc.)
* `generate_lead` (Fired upon successful form submission in the ServiceFormWizard).

---
*Generated by Antigravity AI*
